import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PromptVault — Professional AI Prompt Marketplace",
  description:
    "Battle-tested AI prompt systems for developers, game devs, and marketers. Stop wasting time on bad AI outputs.",
  openGraph: {
    title: "PromptVault — Professional AI Prompt Marketplace",
    description:
      "Battle-tested AI prompt systems for developers, game devs, and marketers.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-[#0a0a0a]">{children}</body>
    </html>
  );
}
